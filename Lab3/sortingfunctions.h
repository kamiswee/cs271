#include <ctype.h>

#include <stdlib.h>

#ifndef SORTINGFUNCTIONS
#define SORTINGFUNCTIONS
    // prototypes for helperfunctions here
    int selectionSort ( int * const data, size_t length );
    int insertionSort ( int * const data, size_t length );
#endif
